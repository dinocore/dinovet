%fieldset{:id => "phone-number-#{phone_number_counter}", :class => "phone-number"}
  = fields_for 'client[phone_numbers]', phone_number, |
    :index => phone_number_counter  do |f|

    = f.select :type, PhoneNumber::Types
    = f.hidden_field :id
    = f.text_field :number
